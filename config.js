window.HUMAN_EVAL = {
  // Paste the Ring500 Apps Script web-app URL here after you deploy (ends with /exec).
  // Use a NEW Google Sheet (not the 70-pilot one): Extensions -> Apps Script,
  // paste Code.gs from this repo, run bindSheet, Deploy -> New version -> Anyone.
  scriptUrl: "",
};
